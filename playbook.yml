---
- name: "Web Dev headless server with Nginx, HHVM, MariaDB, Composer, NodeJS WordPress, WP-CLI, phpMyAdmin"
  hosts: web-dev
  sudo: yes
  remote_user: lamosty

  roles:
  - common
  - hhvm
  - composer
  - mariadb
  - nginx
  - nodejs